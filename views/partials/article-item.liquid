{%- if article -%}
  <article>
    <h2>
      <a href="{{ article.url | url }}">{{ article.data.title }}</a>
    </h2>
    <p>
      {{ 'Posted on ' | escape }}
      <time data-timestamp="{{ article.data.timestamp }}">{{ article.data.timestamp }}</time>
    </p>
  </article>
{%- endif -%}